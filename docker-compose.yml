services:
  immich-person-review:
    build: .
    image: immich-person-review:0.11.0
    container_name: immich-person-review
    restart: unless-stopped
    ports:
      - "3030:3000"
    env_file:
      - .env
    environment:
      PORT: "3000"
      TAG_TAXONOMY_PATH: /app/data/tags.json
    volumes:
      - tag-taxonomy:/app/data
    networks:
      - immich

volumes:
  tag-taxonomy:

networks:
  immich:
    external: true
    name: immich_default
